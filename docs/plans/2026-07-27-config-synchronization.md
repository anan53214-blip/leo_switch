# 配置统一实施清单

日期：2026-07-27

1. 增加配置一致性测试，确认旧默认值会触发失败。
2. 将 `EnvConfig.max_steps` 改为 `600`。
3. 让 `TrainConfig` 继承 `EnvConfig` 的环境与奖励默认值。
4. 同步训练、评估、保存、图更新和模型选优默认值。
5. 让训练 CLI 和基线比较入口复用配置类，不再重复写奖励常量。
6. 更新当前配置文档，并在实验日志追加本次变更。
7. 运行定向测试、完整测试和过时配置搜索。

